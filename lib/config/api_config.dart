class ApiConfig {
  /// Base URL for the backend API
  /// Change this to your local server IP (e.g., 'http://[IP_ADDRESS]/api') during development
  static const String baseUrl = 'https://api.escrow.pesacrow.top/api';
}
